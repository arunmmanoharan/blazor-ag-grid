namespace BlazorAgGrid
{
    /// <summary>
    /// Built-in ag-Grid Theming API themes (v33+). Maps to the corresponding
    /// theme object registered in the interop module.
    /// </summary>
    public enum GridTheme
    {
        Quartz = 0,
        Balham = 1,
        Alpine = 2,
        Material = 3,
    }
}
